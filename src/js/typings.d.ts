interface Window {
	ENAMEL_PIN_CLUB: {
		debugMode: boolean;
		isLoggedIn: boolean;
		root: string;
		dark: boolean;
	};
}

interface JQuery {
	foundation(method?: string | Array<any>, ...args: any[]): JQuery;
}
